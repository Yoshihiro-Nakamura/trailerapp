<%= form_for(@kanri) do |f| %>
  <% if @kanri.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kanri.errors.count, "error") %> prohibited this kanri from being saved:</h2>

      <ul>
      <% @kanri.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
